services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3123:3123"
    env_file:
      - .env
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
